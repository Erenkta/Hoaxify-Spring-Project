#spring:
#  datasource:
#    url: jdbc:h2:./devdb#  jpa:#    hibernate:#      ddl-auto: update
spring:    jpa:      properties:        javax:
          persistence:
            validation:
              mode: none
   data:
      web:
        pageable:
          page-parameter: currentPage
          size-parameter: pageSize #Custom değişkenler verdik 
          default-page-size: 10
          max-page-size: 100     
  profiles:
    active:
      -dev
   
#hoaxify:
  #upload-path: picture-storage
  
  #Profile Configuration yapacağız yani farklı çalışma ortamları için farklı profiller gibi düşünebilirsin
  #Profile oluşturmak için --- kullan
  #Sıralamada aynı fieldlar varsa en son gelen'in field'ı geçerli olur
---
spring:
  profiles: production
  hoaxify:
    upload-path: storage-production
---
spring:
  profiles: dev
  hoaxify:
    upload-path: storage-dev


  